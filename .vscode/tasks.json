{
    "tasks": [
        {
            "type": "shell",
            "label": "build_executable",
            "command": "make DEBUG=1",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}/04/ex01"  // Replace with the correct path
            },
            "detail": "Task to build the executable using make."
        }
    ],
    "version": "2.0.0"
}
